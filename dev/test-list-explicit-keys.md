# Test list — Explicit keys (?)

Seeded before coding by asking: "what tests, when passing, would prove this works?"
Conventions: `[ ]` todo · `[>]` in progress (only one at a time) · `[x]` done ·
add new tests/worries/refactorings as they occur instead of chasing them.
Done when every box is `[x]`.

## To do

## In progress

## Done
- [x] ? key followed by : value on next line
- [x] ? key : value on same line (inline)
- [x] ? key with implicit null value (: with no value)
- [x] ? with no key text creates null key

## Parked (worries / refactorings noticed, not yet tests)
- multiline explicit keys (complex keys spanning lines)
- explicit keys with quoted values
- explicit keys with block scalar values
