 Now, what the heck are we making? A yammer. Ah, we are making a woman. A yamel parsing and emitting library in Common Lisp. Is that everything? That's it. So, so to be a yamel parser, why is that complex? And what does it mean to be a YAML parser? It can just pull values out of YAML fields? It means that it can take a YAMO document and turn it into an internal data structure with a hash table and a list, like a Python dictionary and a Python list. But we're not in Python. We're in common Lisp. So that's a hash table and a vector. Hach table and a vector. Okay. Hash tables and vectors. So why does, so I'm new to this. I don't understand any of this. You're using a YAML file as a data structure. So you could use it in the place of a database for something small. Precisely so. That's what this is for. Is to make a YAML file robust as a, as a storage thing. I mean, that's the purpose of YAML. just a prettier JSON. Okay. All right. We got it.